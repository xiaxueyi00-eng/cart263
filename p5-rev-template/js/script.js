"use strict";

function setup() {
    console.log("go")

    let x, y, w, h;
    let r, g, b;

    x = 100;
    y = 100;
    w = 50;
    h = 50;
    r = 255;
    g = 0;
    b = 0;

    fill(r, g, b);
    ellipse(x, y, w, h);

}
draw

function draw() {

}